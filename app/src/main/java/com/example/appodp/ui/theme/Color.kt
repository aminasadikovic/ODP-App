package com.example.appodp.ui.theme

import androidx.compose.ui.graphics.Color

// Light Theme Colors (existing)
val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)

val DarkPrimary = Color(0xFF6A1B9A) // Duboka, bogata ljubičasta
val DarkOnPrimary = Color(0xFFFFFFFF) // Bijeli tekst na primarnoj boji

val DarkPrimaryContainer = Color(0xFF8E24AA) // Malo svjetlija ljubičasta za kontejnere
val DarkOnPrimaryContainer = Color(0xFFFFFFFF) // Bijeli tekst na primarnom kontejneru

val DarkSecondary = Color(0xFF1976D2) // Duboka, bogata plava
val DarkOnSecondary = Color(0xFFFFFFFF) // Bijeli tekst na sekundarnoj boji

val DarkSecondaryContainer = Color(0xFF2196F3) // Malo svjetlija plava za kontejnere
val DarkOnSecondaryContainer = Color(0xFFFFFFFF) // Bijeli tekst na sekundarnom kontejneru

// Tercijarne boje - za akcente
val DarkTertiary = Color(0xFF00BCD4) // Akcentna boja (npr. tirkizna)
val DarkOnTertiary = Color(0xFF000000) // Crni tekst na akcentnoj boji

val DarkTertiaryContainer = Color(0xFF26C6DA) // Svjetlija akcentna boja za kontejnere
val DarkOnTertiaryContainer = Color(0xFF000000) // Crni tekst na akcentnom kontejneru

// Boje pozadine i površina
val DarkBackground = Color(0xFF121212) // Vrlo tamno siva, često korištena za pozadinu u tamnim temama
val DarkOnBackground = Color(0xFF000000) // PROMIJENJENO: Crni tekst na tamnoj pozadini (može biti nečitljivo)
val DarkSurface = Color(0xFF121212) // Ista kao pozadina za konzistentnost
val DarkOnSurface = Color(0xFF000000) // PROMIJENJENO: Crni tekst na tamnoj površini (može biti nečitljivo)

// Boje za kartice i povišene površine (kao što je traženo - bijele karte sa crnim tekstom)
val DarkSurfaceVariant = Color(0xFFFFFFFF) // Bijela za kartice
val DarkOnSurfaceVariant = Color(0xFF000000) // Crni tekst na bijelim karticama (ovo je već bilo crno)

// Ostale Material3 boje
val DarkError = Color(0xFFCF6679) // Standardna crvena za greške
val DarkOnError = Color(0xFF000000) // Crni tekst na boji greške

val DarkErrorContainer = Color(0xFFB00020) // Tamniji kontejner za greške
val DarkOnErrorContainer = Color(0xFFFFFFFF) // Bijeli tekst na kontejneru za greške

val DarkOutline = Color(0xFF424242) // Tamno siva za obrise

val DarkInverseOnSurface = Color(0xFF121212) // Koristi se za inverzne elemente
val DarkInverseSurface = Color(0xFFE0E0E0) // Koristi se za inverzne elemente
val DarkInversePrimary = Color(0xFFBB86FC) // Inverzna primarna, obično svjetlija verzija primarne
val DarkScrim = Color(0xFF000000) // Scrim za modale/dijaloge
